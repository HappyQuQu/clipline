services:
  clipline:
    build: .
    container_name: clipline
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - C:/Users/EvanQ/Desktop/200video:/recordings/200video:ro
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped

